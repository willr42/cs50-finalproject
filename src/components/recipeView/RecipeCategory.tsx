// A small pill that sits at the top of the RecipeCategory and scrolls horizontally.
const RecipeCategory = () => {
  return <div className='recipe-category'></div>;
};

export default RecipeCategory;
export {};
